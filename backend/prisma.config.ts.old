import { defineConfig, env } from "prisma/config";

export default defineConfig({
  schema: "prisma/schema.prisma",
  datasource: {
    adapter: "postgresql",        // pour connexion directe
    url: env("DATABASE_URL"),     // URL de la base
  },
});
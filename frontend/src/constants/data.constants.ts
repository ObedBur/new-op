import { PriceData } from '@/features/admin-dashboard/types';

/**
 * Données temporaires pour le graphique de prix.
 * À remplacer par un endpoint backend dans une prochaine itération.
 */
export const CHART_DATA: PriceData[] = [];
